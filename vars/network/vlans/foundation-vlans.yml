---
# VLAN examples for tenant and service attachment.
# These objects are referenced by interface, lag, and FDB configuration in the network domain.
# Supported fields: `name`, `vlan_id`, and `state`.
vlans:
  - name: tenant-frontend
    vlan_id: 110
    state: present
