---
# DNS server examples for system name resolution.
# These objects are consumed by the system playbook and may be referenced by authentication workflows.
# Supported fields: `name`, `servers`, `search`, and `state`.
dns_servers:
  - name: primary-dns
    servers:
      - 192.0.2.53
      - 192.0.2.54
    search:
      - example.com
    state: present
