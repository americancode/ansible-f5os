---
# User examples for local platform administrators.
# These objects are consumed by the system playbook.
# Supported fields: `username`, `role`, `expiry_status`, and `state`.
users:
  - username: platformadmin
    role: admin
    expiry_status: enabled
    state: present
